name: Build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: Swift build
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Build
        run: swift build -c debug
